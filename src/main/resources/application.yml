server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  jpa:
    show-sql: true
  messages:
    basename: messages
  security:
    user:
      name: username
      password: passw0rd

# /*** REST API Monitoring을 위한 Actuator 설정 */
management:
  endpoints:
    web:
      exposure:
        include: "*"
